databases:
  - name: sistema-cup-db
    databaseName: sistemacup
    user: sistemacupuser
    plan: free

services:
  - type: web
    name: sistema-cup-web
    env: php
    plan: free
    buildCommand: "./render-build.sh"
    envVars:
      - key: APP_NAME
        value: "SistemaCUP"
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        generateValue: true
      - key: APP_URL
        value: "https://sistema-cup-web.onrender.com"
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: sistema-cup-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: sistema-cup-db
          property: port
      - key: DB_DATABASE
        fromDatabase:
          name: sistema-cup-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: sistema-cup-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: sistema-cup-db
          property: password
      - key: CACHE_DRIVER
        value: file
      - key: SESSION_DRIVER
        value: database
      - key: QUEUE_CONNECTION
        value: sync
      - key: LOG_CHANNEL
        value: stderr
      # Agregar manualmente en el dashboard de Render:
      # STRIPE_KEY, STRIPE_SECRET, STRIPE_WEBHOOK_SECRET
